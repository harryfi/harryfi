@using System.Globalization
@using MasterOnline.Models
@using MasterOnline.ViewModels
@model BarangViewModel
@{
    ViewBag.Title = "Barang";
    Layout = "~/Views/Shared/_LayoutManage.cshtml";
}
@section styles
{
    <link href="~/Content/build/css/bootstrap-datepicker.min.css" rel="stylesheet" />
    <link href="~/Content/selectize.css" rel="stylesheet" />
    <link href="~/Content/build/css/selectivity-jquery.min.css" rel="stylesheet" />
    <style>


        .sub_title {
            font-size: 16px;
        }

        .block_subtitle_label {
            display: inline-block;
            width: 135px;
            font-weight: bold;
        }

        .user-link {
            text-decoration: underline;
        }

        #div-scroll-list {
            overflow-x: auto;
            overflow-y: auto;
            height: 400px;
        }

        #Stf02_Deskripsi {
            resize: vertical;
            height: 100px;
            max-height: 150px;
        }

        .satuan {
            text-align: left !important;
            font-size: 10px;
        }

        #hapus_label {
            color: orange;
            font-weight: bold;
        }

        .foto_produk_section {
            padding: 10px;
        }

            .foto_produk_section > .box_upload {
                width: 250px;
                height: 250px;
                border: dashed 1.5px #bbb;
                border-radius: 10px;
                background-color: #f7f7f7;
                background-image: url('');
                text-align: center;
                display: table;
                margin: 0 auto;
                cursor: pointer;
            }

        .box_upload > .box_upload_placeholder {
            color: #ccc;
            font-size: 20px;
            display: table-cell;
            vertical-align: middle;
        }

        .btn_hapus_foto_produk {
            position: absolute;
            bottom: 15px;
            left: 48px;
        }

        /* =============== Checkbox ================ */

        /* Customize the label (the container) */
        .checkbox-container {
            display: block;
            position: relative;
            padding-left: 35px;
            margin-top: 4px;
            cursor: pointer;
            font-size: 11px;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

            /* Hide the browser's default checkbox */
            .checkbox-container input {
                position: absolute;
                opacity: 0;
                cursor: pointer;
            }

        /* Create a custom checkbox */
        .checkmark {
            position: absolute;
            top: 0;
            left: 0;
            height: 25px;
            width: 25px;
            background-color: #eee;
            border: solid 1px #ccc;
        }

        /* On mouse-over, add a grey background color */
        .checkbox-container:hover input ~ .checkmark {
            background-color: #ccc;
        }

        /* When the checkbox is checked, add a blue background */
        .checkbox-container input:checked ~ .checkmark {
            background-color: #2196F3;
        }

        /* Create the checkmark/indicator (hidden when not checked) */
        .checkmark:after {
            content: "";
            position: absolute;
            display: none;
        }

        /* Show the checkmark when checked */
        .checkbox-container input:checked ~ .checkmark:after {
            display: block;
        }

        /* Style the checkmark/indicator */
        .checkbox-container .checkmark:after {
            left: 9px;
            top: 5px;
            width: 6px;
            height: 10px;
            border: solid white;
            border-width: 0 3px 3px 0;
            -webkit-transform: rotate(45deg);
            -ms-transform: rotate(45deg);
            transform: rotate(45deg);
        }

        [data-tip] {
            position: relative;
        }

            [data-tip]:before {
                content: '';
                /* hides the tooltip when not hovered */
                display: none;
                content: '';
                border-left: 5px solid transparent;
                border-right: 5px solid transparent;
                border-bottom: 5px solid #1a1a1a;
                position: absolute;
                top: 30px;
                /*left: 35px;*/
                z-index: 8;
                font-size: 0;
                line-height: 0;
                /*width: 0;
                height: 0;*/
                margin-left: -30px;
                left: 50%;
            }

            [data-tip]:after {
                display: none;
                content: attr(data-tip);
                position: absolute;
                top: 35px;
                /*left: 0px;*/
                padding: 2px 5px;
                background: #1a1a1a;
                color: #fff;
                z-index: 9;
                font-size: 0.85em;
                height: auto;
                line-break: normal;
                max-width: 300px;
                line-height: 18px;
                -webkit-border-radius: 3px;
                -moz-border-radius: 3px;
                border-radius: 3px;
                white-space: normal;
                word-wrap: normal;
                left: 30px;
                margin-left: -50px;
                width: 68px;
            }

            [data-tip]:hover:before,
            [data-tip]:hover:after {
                display: block;
            }

        [data-tip-stok] {
            position: relative;
        }

            [data-tip-stok]:before {
                content: '';
                /* hides the tooltip when not hovered */
                display: none;
                content: '';
                border-left: 5px solid transparent;
                border-right: 5px solid transparent;
                border-bottom: 5px solid #1a1a1a;
                position: absolute;
                top: 30px;
                z-index: 8;
                font-size: 0;
                line-height: 0;
                left: 30%;
                margin-left: -30px;
            }

            [data-tip-stok]:after {
                display: none;
                content: attr(data-tip-stok);
                position: absolute;
                top: 35px;
                padding: 2px 5px;
                background: #1a1a1a;
                color: #fff;
                z-index: 1;
                font-size: 0.85em;
                height: auto;
                line-break: normal;
                max-width: 300px;
                line-height: 18px;
                -webkit-border-radius: 3px;
                -moz-border-radius: 3px;
                border-radius: 3px;
                white-space: normal;
                word-wrap: normal;
                left: 50px;
                margin-left: -80px;
                width:68px;
            }

            [data-tip-stok]:hover:before,
            [data-tip-stok]:hover:after {
                display: block;
            }

        #modal-detail-pesanan {
            margin: auto;
        }

            #modal-detail-pesanan .modal-content .modal-header {
                -webkit-box-shadow: none;
                box-shadow: none;
                border: none;
                background-color: white;
            }

            #modal-detail-pesanan .modal-content .modal-body {
                height: 125px;
                -webkit-box-shadow: none;
                box-shadow: none;
                background-color: white;
            }
    </style>
}
<div class="row">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="" role="tabpanel" data-example-id="togglable-tabs">
            <ul id="myTab" class="nav nav-tabs" role="tablist">
                <li role="presentation" class="active">
                    <a href="#tab_content1" id="barang-tab" role="tab" data-toggle="tab" aria-expanded="true">Barang</a>
                </li>
                <li role="presentation" class="">
                    <a href="#tab_content2" role="tab" id="kosong-tab" data-toggle="tab" aria-expanded="false">Barang Kosong</a>
                </li>
                <li role="presentation" class="">
                    <a href="#tab_content4" role="tab" id="tidaklaku-tab" data-toggle="tab" aria-expanded="false">Barang Tidak Laku</a>
                </li>
                <li role="presentation" class="">
                    <a href="#tab_content6" role="tab" id="minimumstok-tab" data-toggle="tab" aria-expanded="false">Barang di Bawah Minimum Stok</a>
                </li>
                <li role="presentation" class="">
                    <a href="#tab_content7" role="tab" id="palinglaku-tab" data-toggle="tab" aria-expanded="false">Barang Paling Laku</a>
                </li>
            </ul>
            <div id="myTabContent" class="tab-content" style="padding-top:10px">
                <div role="tabpanel" class="tab-pane fade active in" id="tab_content1" aria-labelledby="barang-tab">
                    <div class="row barang_table_section">
                        <div class="col-lg-4 col-sm-6">
                            <div class="input-group">
                                <input id="search_barang" type="text" class="form-control" placeholder="Pencarian">
                                <span class="input-group-btn">
                                    <button type="button" id="search_barang_click" class="btn btn-primary">
                                        <i class="fa fa-search"></i>
                                    </button>
                                </span>
                            </div>
                        </div>
                        @*<div class="col-lg-8 col-sm-6">
                                <div class="pull-right">
                                    <button type="button" class="btn btn-default" data-style="expand-right">
                                        <span>Refresh</span>
                                        </button>
                                </div>
                            </div>*@
                    </div>
                    <div class="row" style="margin-top: 5px;">
                        @* remark by calvin 24 mei 2019 <div id="loading_barang_tab" class="text-center">
                                <img src="~/Content/Images/spinner.gif" width="100" />
                            </div>*@
                        <div class="col-sm-12 barang_table_section" id="table-barang-1-partial">
                            @* remark by calvin 24 mei 2019 @Html.Partial("TableMenuBarang1Partial")*@
                        </div>
                    </div>
                    <div class="row barang_editor_section" style="display: none;">
                        <div class="col-lg-12">
                            <div class="page-editor">
                                <h2 class="editor-title">Barang</h2>
                                <span class="title-accent"></span>
                                <button id="close-editor-1" type="button" class="pull-right page-close">
                                    <span class="close-btn thick"></span>
                                </button>
                                @if (ViewData.ModelState.Any(x => x.Value.Errors.Any()))
                                {
                                    foreach (var modelError in ViewData.ModelState.SelectMany(x => x.Value.Errors))
                                    {
                                        <div class="alert alert-danger">
                                            <span class="message-error">@modelError.ErrorMessage</span>
                                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                                <span aria-hidden="true">&times;</span>
                                            </button>
                                        </div>
                                    }
                                }
                                @using (Html.BeginForm("SaveBarang", "Manage", FormMethod.Post, new { enctype = "multipart/form-data", id = "form-barang" }))
                                {
                                    <div id="form-partial">
                                        @* remark by calvin 24 mei 2019 @Html.Partial("FormBarangPartial")*@
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                </div>
                <div role="tabpanel" class="tab-pane fade" id="tab_content2" aria-labelledby="kosong-tab">
                    <div class="row">
                        <div class="col-lg-4 col-sm-6">
                            <div class="input-group">
                                <input id="search_kosong" type="text" class="form-control" placeholder="Pencarian">
                                <span class="input-group-btn">
                                    <button type="button" id="search_kosong_click" class="btn btn-primary">
                                        <i class="fa fa-search"></i>
                                    </button>
                                </span>
                            </div>
                        </div>
                        @*<div class="col-lg-8 col-sm-6">
                                <div class="pull-right">
                                    <button type="button" class="btn btn-default" data-style="expand-right">
                                            <span>Refresh</span>
                                        </button>
                                </div>
                            </div>*@
                    </div>
                    <div class="row" style="margin-top: 5px;">
                        @*<div id="loading_kosong_tab" class="text-center">
                                <img src="~/Content/Images/spinner.gif" width="100" />
                            </div>*@
                        <div class="col-sm-12" id="table-barang-kosong-partial">
                            @* remark by calvin 24 mei 2019 @Html.Partial("TableBarangKosongPartial", new List<PenjualanBarang>())*@
                        </div>
                    </div>
                </div>
                <div role="tabpanel" class="tab-pane fade" id="tab_content4" aria-labelledby="tidaklaku-tab">
                    <div class="row">
                        <div class="col-lg-4 col-sm-6">
                            <div class="input-group">
                                <input id="search_tidaklaku" type="text" class="form-control" placeholder="Pencarian">
                                <span class="input-group-btn">
                                    <button type="button" id="search_tidaklaku_click" class="btn btn-primary">
                                        <i class="fa fa-search"></i>
                                    </button>
                                </span>
                            </div>
                        </div>
                        @*<div class="col-lg-8 col-sm-6">
                                <div class="pull-right">
                                    <button type="button" class="btn btn-default" data-style="expand-right">
                                            <span>Refresh</span>
                                        </button>
                                </div>
                            </div>*@
                    </div>
                    <div class="row">
                        <div class="col-md-2">
                            @Html.Label("", "Dari Tanggal")
                            <div class="input-group date">
                                @*@Html.TextBox("DrTanggal", null, new { @class = "form-control", @disabled = "disable", Value = DateTime.Now.AddMonths(-3).ToString("dd/MM/yyyy") })*@
                                @Html.TextBox("DrTanggal", null, new { @class = "form-control", @ReadOnly = true })
                                <span class="input-group-addon">
                                    <span class="glyphicon glyphicon-calendar"></span>
                                </span>
                            </div>
                        </div>
                        <div class="col-md-2">
                            @Html.Label("", "S/d Tanggal")
                            <div class="input-group date">
                                @*@Html.TextBox("SdTanggal", null, new { @class = "form-control", @disabled = "disable", Value = DateTime.Now.ToString("dd/MM/yyyy") })*@
                                @Html.TextBox("SdTanggal", null, new { @class = "form-control", @ReadOnly = true })
                                <span class="input-group-addon">
                                    <span class="glyphicon glyphicon-calendar"></span>
                                </span>
                            </div>
                        </div>
                        <div class="col-md-8">
                        </div>
                    </div>
                    <div class="row" style="margin-top: 5px;">
                        @*<div id="loading_tidak_laku_tab" class="text-center">
                                <img src="~/Content/Images/spinner.gif" width="100" />
                            </div>*@
                        <div class="col-sm-12" id="table-barang-tidak-laku-partial">
                            @* remark by calvin 24 mei 2019 @Html.Partial("TableBarangTidakLakuPartial", new List<PenjualanBarang>())*@
                        </div>
                    </div>
                </div>
                <div role="tabpanel" class="tab-pane fade" id="tab_content6" aria-labelledby="minimumstok-tab">
                    <div class="row">
                        <div class="col-lg-4 col-sm-6">
                            <div class="input-group">
                                <input id="search_minimumstok" type="text" class="form-control" placeholder="Pencarian">
                                <span class="input-group-btn">
                                    <button type="button" id="search_minimumstok_click" class="btn btn-primary">
                                        <i class="fa fa-search"></i>
                                    </button>
                                </span>
                            </div>
                        </div>
                        @*<div class="col-lg-8 col-sm-6">
                                <div class="pull-right">
                                    <button type="button" class="btn btn-default" data-style="expand-right">
                                            <span>Refresh</span>
                                        </button>
                                </div>
                            </div>*@
                    </div>
                    <div class="row">
                        <div class="col-md-2">
                            @Html.Label("", "Dari Tanggal")
                            <div class="input-group date">
                                @*@Html.TextBox("DrTanggal", null, new { @class = "form-control", @disabled = "disable", Value = DateTime.Now.AddMonths(-3).ToString("dd/MM/yyyy") })*@
                                @Html.TextBox("DrTanggal2", null, new { @class = "form-control", @ReadOnly = true })
                                <span class="input-group-addon">
                                    <span class="glyphicon glyphicon-calendar"></span>
                                </span>
                            </div>
                        </div>
                        <div class="col-md-2">
                            @Html.Label("", "S/d Tanggal")
                            <div class="input-group date">
                                @*@Html.TextBox("SdTanggal", null, new { @class = "form-control", @disabled = "disable", Value = DateTime.Now.ToString("dd/MM/yyyy") })*@
                                @Html.TextBox("SdTanggal2", null, new { @class = "form-control", @ReadOnly = true })
                                <span class="input-group-addon">
                                    <span class="glyphicon glyphicon-calendar"></span>
                                </span>
                            </div>
                        </div>
                        @*//ADD BY NURUL 25/9/2019*@
                        <div class="col-md-2">
                            @Html.Label("", "Urut Berdasarkan")
                            <div class="input-group text">
                                <select class="form-control" id="Order" name="Order" required="required">
                                    <option value="1">Qty Terjual</option>
                                    <option value="2">Qty Siap Jual</option>
                                </select>
                            </div>
                        </div>
                        @*//END ADD BY NURUL 25/9/2019*@
                        <div class="col-md-6">
                        </div>
                    </div>
                    <div class="row" style="margin-top: 5px;">
                        <div id="loading_minimum_stok_tab" class="text-center">
                            <img src="~/Content/Images/spinner.gif" width="100" />
                        </div>
                        <div class="col-sm-12" id="table-barang-minimum-stok-partial">
                            @* remark by calvin 24 mei 2019 @Html.Partial("TableBarangDibawahMinimumStokPartial", new List<PenjualanBarang>())*@
                        </div>
                    </div>
                </div>
                <div role="tabpanel" class="tab-pane fade" id="tab_content7" aria-labelledby="palinglaku-tab">
                    <div class="row">
                        <div class="col-lg-4 col-sm-6">
                            <div class="input-group">
                                <input id="search_palinglaku" type="text" class="form-control" placeholder="Pencarian">
                                <span class="input-group-btn">
                                    <button type="button" id="search_palinglaku_click" class="btn btn-primary">
                                        <i class="fa fa-search"></i>
                                    </button>
                                </span>
                            </div>
                        </div>
                        @*<div class="col-sm-3">
                                <h4 style="border-bottom:solid; ">Periode 3 Bulan Terakhir</h4>
                            </div>*@
                        @*<div class="col-lg-8 col-sm-6">
                                <div class="pull-right">
                                    <button type="button" class="btn btn-default" data-style="expand-right">
                                            <span>Refresh</span>
                                        </button>
                                </div>
                            </div>*@
                    </div>
                    <div class="row">
                        <div class="col-md-2">
                            @Html.Label("", "Dari Tanggal")
                            <div class="input-group date">
                                @*@Html.TextBox("DrTanggal1", null, new { @class = "form-control", @disabled = "disable", Value = DateTime.Now.AddMonths(-3).ToString("dd/MM/yyyy") })*@
                                @Html.TextBox("DrTanggal1", null, new { @class = "form-control", @ReadOnly = true })
                                <span class="input-group-addon">
                                    <span class="glyphicon glyphicon-calendar"></span>
                                </span>
                            </div>
                        </div>
                        <div class="col-md-2">
                            @Html.Label("", "S/d Tanggal")
                            <div class="input-group date">
                                @*@Html.TextBox("SdTanggal1", null, new { @class = "form-control", @disabled = "disable", Value = DateTime.Now.ToString("dd/MM/yyyy") })*@
                                @Html.TextBox("SdTanggal1", null, new { @class = "form-control", @ReadOnly = true })
                                <span class="input-group-addon">
                                    <span class="glyphicon glyphicon-calendar"></span>
                                </span>
                            </div>
                        </div>
                        <div class="col-md-8">
                        </div>
                    </div>
                    <div class="row" style="margin-top: 5px;">
                        @*<div id="loading_palinglaku_tab" class="text-center">
                                <img src="~/Content/Images/spinner.gif" width="100" />
                            </div>*@
                        <div class="col-sm-12" id="table-barang-paling-laku-partial">
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Modal Hapus customer -->
<div class="modal fade" id="konfHapusBarang" tabindex="-1" role="dialog" aria-labelledby="konfirmasiHapusLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="konfirmasiHapusLabel">Konfirmasi</h4>
            </div>
            <div class="modal-body">
                <div class="row text-center">
                    <h4>Apakah Anda yakin ingin menghapus barang <span id="hapus_label"></span>?</h4>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Tidak</button>
                <button type="button" class="btn btn-success" onclick="deleteBarang()">Ya</button>
            </div>
        </div>
    </div>
</div>
@*//add by nurul 29/4/2020*@
<div class="modal fade" id="modalDetailPesanan" style="margin:0;padding:0;" tabindex="-1" role="dialog" aria-labelledby="modalDetailPesananLabel">
    <div class="modal-dialog" role="document" style="margin-left:200px;">
        <div class="modal-content" style="width:950px;">
            <div class="modal-header">
                <h4 class="modal-title" id="modalDetailPesananLabel">Detail Qty Pesanan</h4>

                <div class="row" @*style="padding-left:15px"*@>
                    @*<span class="control-label col-md-2 col-sm-2">Kode Barang </span><span id="kd-brg" class="col-md-9 col-sm-9 control-label" disabled = "disabled"></span>*@
                    <div class="form-group" style="margin-top: 5px;">
                        <label class="control-label col-md-2 col-sm-2">Kode Barang</label>
                        <div class="col-md-3 col-sm-3">
                            @*<span id="kd-brg" class="control-label" disabled="disabled"></span>*@
                            @*<text id="kd-brg" class="control-label" disabled="disabled">Kode Barang</text>*@
                            @Html.TextBox("kd-brg", "", new { @class = "form-control", disabled = "disabled" })
                        </div>
                    </div>
                </div>
                <div class="row" @*style="padding-left:15px"*@>
                    @*<span class="control-label col-md-2 col-sm-2">Nama Barang </span><span id="nm-brg" class="col-md-9 col-sm-9 control-label" disabled = "disabled"></span>*@
                    <div class="form-group" style="margin-top: 5px;">
                        <label class="control-label col-md-2 col-sm-2">Nama Barang</label>
                        <div class="col-md-10 col-sm-10">
                            @*<span id="nm-brg" class="control-label" disabled="disabled"></span>*@
                            @*<text id="nm-brg" class="control-label" disabled="disabled">Kode Barang</text>*@
                            @Html.TextArea("nm-brg", "", new { @class = "form-control", disabled = "disabled" })
                        </div>
                    </div>
                </div>
                <div class="row" @*style="padding-left:15px"*@>
                    @*<span class="control-label col-md-2 col-sm-2">Total Qty Pesanan </span><span id="ttl-qty" class="col-md-9 col-sm-9 control-label" disabled = "disabled"></span>*@
                    <div class="form-group" style="margin-top: 5px;">
                        <label class="control-label col-md-2 col-sm-2">Total Qty Pesanan</label>
                        <div class="col-md-10 col-sm-10">
                            @*@Html.Label("ttl-qty", "", new { @class = "control-label", disabled = "disabled" })*@
                            @*<span id="ttl-qty" class="control-label" disabled="disabled"></span>*@
                            <label id="ttl-qty" class="control-label" disabled="disabled"></label>
                        </div>
                    </div>
                </div>
                <div class="row" @*style="padding-left:15px"*@>
                    @*<span class="control-label col-md-2 col-sm-2">Total Pesanan </span><span id="ttl-pesanan" class="col-md-9 col-sm-9 control-label" disabled = "disabled"></span>*@
                    <div class="form-group" style="margin-top: 5px;">
                        <label class="control-label col-md-2 col-sm-2">Total Pesanan</label>
                        <div class="col-md-10 col-sm-10">
                            @*<span id="ttl-pesanan" class="control-label" disabled="disabled"></span>*@
                            <label id="ttl-pesanan" class="control-label" disabled="disabled"></label>
                            @*@Html.Label("ttl-pesanan", "", new { @class = "control-label", disabled = "disabled" })*@
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-lg-4 col-sm-6" style="margin-bottom:7px;">
                        <div class="input-group">
                            <input id="search_detail" type="text" class="form-control" placeholder="Pencarian">
                            <span class="input-group-btn">
                                <button type="button" id="search_detail_click" class="btn btn-primary">
                                    <i class="fa fa-search"></i>
                                </button>
                            </span>
                        </div>
                    </div>
                </div>
                <div id="list-detail"></div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" id="close-detail" data-dismiss="modal">Tutup</button>
            </div>
        </div>
    </div>
</div>
@*end add by nurul 29/4/2020*@
@*//add by nurul 13/5/2020*@
<div class="modal fade" id="modalDetailStok" style="margin:0;padding:0;" tabindex="-1" role="dialog" aria-labelledby="modalDetailStokLabel">
    <div class="modal-dialog" role="document" style="margin-left:350px;">
        <div class="modal-content" style="width:700px;">
            <div class="modal-header">
                <h4 class="modal-title" id="modalDetailStokLabel">Detail Stok</h4>

                <div class="row">
                    <div class="form-group" style="margin-top: 5px;">
                        <label class="control-label col-md-2 col-sm-2">Kode Barang</label>
                        <div class="col-md-3 col-sm-3">
                            @Html.TextBox("kd-brg-stok", "", new { @class = "form-control", disabled = "disabled" })
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="form-group" style="margin-top: 5px;">
                        <label class="control-label col-md-2 col-sm-2">Nama Barang</label>
                        <div class="col-md-10 col-sm-10">
                            @Html.TextArea("nm-brg-stok", "", new { @class = "form-control", disabled = "disabled" })
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="form-group" style="margin-top: 5px;">
                        <label class="control-label col-md-2 col-sm-2">Total Stok</label>
                        <div class="col-md-10 col-sm-10">
                            <label id="ttl-stok" class="control-label" disabled="disabled"></label>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="form-group" style="margin-top: 5px;">
                        <label class="control-label col-md-2 col-sm-2">Total Gudang</label>
                        <div class="col-md-10 col-sm-10">
                            <label id="ttl-gd" class="control-label" disabled="disabled"></label>
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-lg-4 col-sm-6" style="margin-bottom:7px;">
                        <div class="input-group">
                            <input id="search_detail_stok" type="text" class="form-control" placeholder="Pencarian">
                            <span class="input-group-btn">
                                <button type="button" id="search_detail_stok_click" class="btn btn-primary">
                                    <i class="fa fa-search"></i>
                                </button>
                            </span>
                        </div>
                    </div>
                </div>
                <div id="list-detail-stok"></div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" id="close-detail-stok" data-dismiss="modal">Tutup</button>
            </div>
        </div>
    </div>
</div>
@*end add by nurul 13/5/2020*@
@section scripts
{
    <script src="~/Content/build/js/bootstrap-datepicker.min.js" type="text/javascript"></script>
    <script src="~/Content/selectize.js" type="text/javascript"></script>
    <script src="~/Content/build/js/selectivity-jquery.min.js" type="text/javascript"></script>
    <script type="text/javascript">
        var tempBrg = "";
        var tempNama = "";
        var tempQty = "";
        var tempPesanan = "";

        var tempBrgStok = "";
        var tempNamaStok = "";
        var tempStok = "";
        var tempGd = "";
        //remark by calvin 24 mei 2019
        //var table = $('#datatable').DataTable();
        //var tableKosong = $('#datatable_kosong').DataTable();
        //var tableTidakLaku = $('#datatable_tidaklaku').DataTable();
        //var tableMinimumStok = $('#datatable_minimumstok').DataTable();
        //var tablePalingLaku = $('#datatable_palinglaku').DataTable();

        //$('#search_barang').keyup(function () {
        //    table.search($(this).val()).draw();
        //});

        //$('#search_palinglaku').keyup(function () {
        //    tablePalingLaku.search($(this).val()).draw();
        //});

        //$('#search_kosong').keyup(function () {
        //    tableKosong.search($(this).val()).draw();
        //});

        //$('#search_tidaklaku').keyup(function () {
        //    tableTidakLaku.search($(this).val()).draw();
        //});

        //$('#search_minimumstok').keyup(function () {
        //    tableMinimumStok.search($(this).val()).draw();
        //});
        //end remark by calvin 24 mei 2019


        $(document).ready(function () {
            var d = new Date();
            d.setMonth(d.getMonth() - 3);
            var e = new Date();
            e.setMonth(e.getMonth() - 1);
            //tidaklaku-tab
            $('#DrTanggal').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).datepicker('setDate', e);
            $('#DrTanggal').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).change(function () {
                $('#DrTanggal').val($('#DrTanggal').val());
                $('#DrTanggal').datepicker('hide');
                refreshTableBarangTidakLaku();
            });
            $('#SdTanggal').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).datepicker('setDate', '0');
            $('#SdTanggal').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).change(function () {
                $('#SdTanggal').val($('#SdTanggal').val());
                $('#SdTanggal').datepicker('hide');
                refreshTableBarangTidakLaku();
            });
            //palinglaku-tab
            $('#DrTanggal1').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).datepicker('setDate', d);
            $('#DrTanggal1').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).change(function () {
                $('#DrTanggal1').val($('#DrTanggal1').val());
                $('#DrTanggal1').datepicker('hide');
                refreshTablePalingLaku();
            });
            $('#SdTanggal1').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).datepicker('setDate', '0');
            $('#SdTanggal1').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).change(function () {
                $('#SdTanggal1').val($('#SdTanggal1').val());
                $('#SdTanggal1').datepicker('hide');
                refreshTablePalingLaku();
            });
            //minimumstok-tab
            $('#DrTanggal2').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).datepicker('setDate', e)
            $('#DrTanggal2').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).change(function () {
                $('#DrTanggal2').val($('#DrTanggal2').val());
                $('#DrTanggal2').datepicker('hide');
                refreshTableMinimumStok();
            });
            $('#SdTanggal2').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).datepicker('setDate', '0');
            $('#SdTanggal2').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).change(function () {
                $('#SdTanggal2').val($('#SdTanggal2').val());
                $('#SdTanggal2').datepicker('hide');
                refreshTableMinimumStok();
            });

            $('#search_barang').keypress(function (e) {
                var key = e.which;
                if (key == 13)// the enter key code
                {
                    searchTableBarang();
                    return false;
                }
            });
            $('#search_barang_click').click(function () {
                searchTableBarang();
            });
            $('#barang-tab').click(function () {
                refreshTableBarang();
            });

            $('#search_kosong').keypress(function (e) {
                var key = e.which;
                if (key == 13)// the enter key code
                {
                    searchTableBarangKosong();
                    return false;
                }
            });
            $('#search_kosong_click').click(function () {
                searchTableBarangKosong();
            });
            $('#kosong-tab').click(function () {
                refreshTableBarangKosong();
            });

            $('#search_tidaklaku').keypress(function (e) {
                var key = e.which;
                if (key == 13)// the enter key code
                {
                    searchTableBarangTidakLaku();
                    return false;
                }
            });
            $('#search_tidaklaku_click').click(function () {
                searchTableBarangTidakLaku();
            });
            $('#tidaklaku-tab').click(function () {
                refreshTableBarangTidakLaku();
            });


            $('#search_palinglaku').keypress(function (e) {
                var key = e.which;
                if (key == 13)// the enter key code
                {
                    searchTablePalingLaku();
                    return false;
                }
            });
            $('#search_palinglaku_click').click(function () {
                searchTablePalingLaku();
            });
            $('#palinglaku-tab').click(function () {
                refreshTablePalingLaku();
            });

            $('#search_minimumstok').keypress(function (e) {
                var key = e.which;
                if (key == 13)// the enter key code
                {
                    searchTableMinimumStok();
                    return false;
                }
            });
            $('#search_minimumstok_click').click(function () {
                searchTableMinimumStok();
            });
            $('#minimumstok-tab').click(function () {
                $('#Order').val("1");
                refreshTableMinimumStok();
            });

            $('#barang-tab').click();

            $('#Order').change(function () {
                refreshTableMinimumStok();
            });

            $('#search_detail').keypress(function (e) {
                var key = e.which;
                if (key == 13)// the enter key code
                {
                    searchTableDetail();
                    return false;
                }
            });
            $('#search_detail_click').click(function () {
                searchTableDetail();
            });

            $('#close-detail').click(function () {
                tempBrg = "";
                tempNama = "";
                tempQty = "";
                tempPesanan = "";
            });

            $('#search_detail_stok').keypress(function (e) {
                var key = e.which;
                if (key == 13)// the enter key code
                {
                    searchTableDetailStok();
                    return false;
                }
            });
            $('#search_detail_stok_click').click(function () {
                searchTableDetailStok();
            });

            $('#close-detail-stok').click(function () {
                tempBrgStok = "";
                tempNamaStok = "";
                tempStok = "";
                tempGd = "";
            });
        });

        function searchTableBarang() {
            var link = '@Html.Raw(Url.Action("RefreshTableMenuBarang", "Manage", new { search = "replaceSearch" }))';
            link = link.replace("replaceSearch", encodeURIComponent($('#search_barang').val()));

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#table-barang-1-partial').hide();
                    $('#loading_barang_tab').show();
                },
                success: function (response) {
                    $('#table-barang-1-partial').html(response);
                    $('#loading_barang_tab').hide();
                    $('#table-barang-1-partial').show();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }

        function refreshTableBarang() {
            var link = '@Html.Raw(Url.Action("RefreshTableMenuBarang", "Manage", new { page = "replaceLastPage", search = "replaceSearch" }))';
            link = link.replace("replaceSearch", encodeURIComponent($('#search_barang').val()));
            link = link.replace("replaceLastPage", encodeURIComponent($('#txt_last_page_tmb1p').val()));

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#table-barang-1-partial').hide();
                    $('#loading_barang_tab').show();
                },
                success: function (response) {
                    $('#table-barang-1-partial').html(response);
                    $('#loading_barang_tab').hide();
                    $('#table-barang-1-partial').show();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }

        function searchTableBarangKosong() {
            var link = '@Html.Raw(Url.Action("RefreshTableBarangKosong", "Manage", new { search = "replaceSearch" }))';
            link = link.replace("replaceSearch", encodeURIComponent($('#search_kosong').val()));

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#table-barang-kosong-partial').hide();
                    $('#loading_kosong_tab').show();
                },
                success: function (response) {
                    $('#table-barang-kosong-partial').html(response);
                    $('#loading_kosong_tab').hide();
                    $('#table-barang-kosong-partial').show();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }
        function refreshTableBarangKosong() {
            var link = '@Html.Raw(Url.Action("RefreshTableBarangKosong", "Manage", new { page = "replaceLastPage", search = "replaceSearch" }))';
            link = link.replace("replaceSearch", encodeURIComponent($('#search_kosong').val()));
            link = link.replace("replaceLastPage", encodeURIComponent($('#txt_last_page_tbkp').val()));

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#table-barang-kosong-partial').hide();
                    $('#loading_kosong_tab').show();
                },
                success: function (response) {
                    $('#table-barang-kosong-partial').html(response);
                    $('#loading_kosong_tab').hide();
                    $('#table-barang-kosong-partial').show();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }

        function searchTableBarangTidakLaku() {
            @*change by nurul 16/1/2019 -- var link = '@Url.Action("RefreshTableBarangTidakLaku", "Manage")'; 'change by nurul 15/1/2019 *@
            var link = '@Html.Raw(Url.Action("RefreshTableBarangTidakLaku", "Manage", new { search = "replaceSearch", param = "replacedrtgl;replacesdtgl" }))';
            link = link.replace("replaceSearch", encodeURIComponent($('#search_tidaklaku').val()));
            link = link.replace("replacedrtgl", encodeURIComponent($('#DrTanggal').val()));
            link = link.replace("replacesdtgl", encodeURIComponent($('#SdTanggal').val()));

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#table-barang-tidak-laku-partial').hide();
                    $('#loading_tidak_laku_tab').show();
                },
                success: function (response) {
                    $('#table-barang-tidak-laku-partial').html(response);
                    $('#loading_tidak_laku_tab').hide();
                    $('#table-barang-tidak-laku-partial').show();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }
        function refreshTableBarangTidakLaku() {
            @*change by nurul 16/1/2019 -- var link = '@Url.Action("RefreshTableBarangTidakLaku", "Manage")'; 'change by nurul 15/1/2019 *@
            var link = '@Html.Raw(Url.Action("RefreshTableBarangTidakLaku", "Manage", new { page = "replaceLastPage", search = "replaceSearch", param = "replacedrtgl;replacesdtgl" }))';
            link = link.replace("replaceSearch", encodeURIComponent($('#search_tidaklaku').val()));
            link = link.replace("replaceLastPage", encodeURIComponent($('#txt_last_page_tbtlp').val()));
            link = link.replace("replacedrtgl", encodeURIComponent($('#DrTanggal').val()));
            link = link.replace("replacesdtgl", encodeURIComponent($('#SdTanggal').val()));

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#table-barang-tidak-laku-partial').hide();
                    $('#loading_tidak_laku_tab').show();
                },
                success: function (response) {
                    $('#table-barang-tidak-laku-partial').html(response);
                    $('#loading_tidak_laku_tab').hide();
                    $('#table-barang-tidak-laku-partial').show();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }

        function searchTablePalingLaku() {
            @*change by nurul 16/1/2019 -- var link = '@Url.Action("RefreshTableBarangTidakLaku", "Manage")'; 'change by nurul 15/1/2019 *@
            var link = '@Html.Raw(Url.Action("RefreshTableBarangPalingLaku", "Manage", new { search = "replaceSearch", param = "replacedrtgl;replacesdtgl" }))';
            link = link.replace("replaceSearch", encodeURIComponent($('#search_palinglaku').val()));
            link = link.replace("replacedrtgl", encodeURIComponent($('#DrTanggal1').val()));
            link = link.replace("replacesdtgl", encodeURIComponent($('#SdTanggal1').val()));

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#table-barang-paling-laku-partial').hide();
                    $('#loading_palinglaku_tab').show();
                },
                success: function (response) {
                    $('#table-barang-paling-laku-partial').html(response);
                    $('#loading_palinglaku_tab').hide();
                    $('#table-barang-paling-laku-partial').show();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }
        function refreshTablePalingLaku() {
            @*change by nurul 16/1/2019 -- var link = '@Url.Action("RefreshTableBarangPalingLaku", "Manage")'; 'change by nurul 15/1/2019*@
            var link = '@Html.Raw(Url.Action("RefreshTableBarangPalingLaku", "Manage", new { page = "replaceLastPage", search = "replaceSearch", param = "replacedrtgl;replacesdtgl" }))';
            link = link.replace("replaceSearch", encodeURIComponent($('#search_palinglaku').val()));
            link = link.replace("replaceLastPage", encodeURIComponent($('#txt_last_page_tbpl').val()));
            link = link.replace("replacedrtgl", encodeURIComponent($('#DrTanggal1').val()));
            link = link.replace("replacesdtgl", encodeURIComponent($('#SdTanggal1').val()));

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#table-barang-paling-laku-partial').hide();
                    $('#loading_palinglaku_tab').show();
                },
                success: function (response) {
                    $('#table-barang-paling-laku-partial').html(response);
                    $('#loading_palinglaku_tab').hide();
                    $('#table-barang-paling-laku-partial').show();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }

        function searchTableMinimumStok() {
            var link = '@Html.Raw(Url.Action("RefreshTableBarangDibawahMinimumStok", "Manage", new { search = "replaceSearch", param = "replacedrtgl;replacesdtgl", order = "replaceOrder" }))';
            link = link.replace("replaceSearch", encodeURIComponent($('#search_minimumstok').val()));
            link = link.replace("replacedrtgl", encodeURIComponent($('#DrTanggal2').val()));
            link = link.replace("replacesdtgl", encodeURIComponent($('#SdTanggal2').val()));
            link = link.replace("replaceOrder", $('#Order').val());
            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#table-barang-minimum-stok-partial').hide();
                    $('#loading_minimum_stok_tab').show();
                },
                success: function (response) {
                    $('#table-barang-minimum-stok-partial').html(response);
                    $('#loading_minimum_stok_tab').hide();
                    $('#table-barang-minimum-stok-partial').show();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }
        function refreshTableMinimumStok() {
            //change by nurul 6/5/2019
            @*var link = '@Url.Action("RefreshTableBarangDibawahMinimumStok", "Manage")';*@
            var link = '@Html.Raw(Url.Action("RefreshTableBarangDibawahMinimumStok", "Manage", new { page = "replaceLastPage", search = "replaceSearch", param = "replacedrtgl;replacesdtgl", order = "replaceOrder" }))';
            link = link.replace("replaceSearch", encodeURIComponent($('#search_minimumstok').val()));
            link = link.replace("replaceLastPage", encodeURIComponent($('#txt_last_page_tbms').val()));
            link = link.replace("replacedrtgl", encodeURIComponent($('#DrTanggal2').val()));
            link = link.replace("replacesdtgl", encodeURIComponent($('#SdTanggal2').val()));
            link = link.replace("replaceOrder", $('#Order').val());
            //end add by nurul 6/5/2019

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#table-barang-minimum-stok-partial').hide();
                    $('#loading_minimum_stok_tab').show();
                },
                success: function (response) {
                    $('#table-barang-minimum-stok-partial').html(response);
                    $('#loading_minimum_stok_tab').hide();
                    $('#table-barang-minimum-stok-partial').show();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }

        function ConvertToAngkaRupiah(source) {
            if (source.value == '') {
                source.value = 0;
            }
            source.value = (convertToAngka(source.value));
            source.value = (convertToRupiah(source.value));
        }

        //add by nurul 29/4/2020
        function LihatDetailPesanan($req_id) {
            if ($req_id != null && $req_id != undefined && $req_id != "") {
            var tempReqId = $req_id.split('|');
            tempBrg = tempReqId[0];
            tempNama = tempReqId[1];
            tempQty = tempReqId[2];
                $('#search_detail').val('');
                $('#txt_last_page_detail').val('')
        var $link = '@Html.Raw(Url.Action("LihatDetailPesanan", "Manage", new { brgId = "replaceID", page = "replaceLastPage", search = "replaceSearch" }))';
            $link = $link.replace("replaceID", encodeURIComponent(tempBrg));
            $link = $link.replace("replaceSearch", encodeURIComponent($('#search_detail').val()));
            $link = $link.replace("replaceLastPage", encodeURIComponent($('#txt_last_page_detail').val()));

        $.ajax({
            type: "GET",
            url: $link,
            beforeSend: function () {
                $('#list-detail').hide();
                $('#loading_detail_barang_tab').show();
                //$('#kd-brg').text(tempBrg);
                //$('#nm-brg').text(tempNama);
                //$('#ttl-qty').text(tempQty);
                $('#kd-brg').val(tempBrg);
                $('#nm-brg').text(tempNama);
                $('#ttl-qty').text(": " + tempQty);
            },
            success: function (value) {
                $('#list-detail').html(value);
                tempPesanan = qtyPesanan;
                //$('#ttl-pesanan').text(tempPesanan);
                $('#ttl-pesanan').text(": " + tempPesanan);
                $('#loading_detail_barang_tab').hide();
                $('#list-detail').show();
            },
            error: function (xhr, status, error) {
                $('#loading_detail_barang_tab').hide();
                alert(error);
                console.log(error);
            }
            });
        } else {
            $('#loading_detail_barang_tab').hide();
            alert("Tidak ada detail pesanan.");
        }
        }

        function searchTableDetail() {
            if (tempBrg != null && tempBrg != undefined && tempBrg != "") {
            var link = '@Html.Raw(Url.Action("LihatDetailPesanan", "Manage", new { brgId = "replaceID", search = "replaceSearch" }))';
            link = link.replace("replaceID", encodeURIComponent(tempBrg));
            link = link.replace("replaceSearch", encodeURIComponent($('#search_detail').val()));

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#list-detail').hide();
                    $('#loading_detail_barang_tab').show();
                },
                success: function (response) {
                    $('#list-detail').html(response);
                    $('#loading_detail_barang_tab').hide();
                    $('#list-detail').show();
                },
                error: function (xhr, status, error) {
                    $('#loading_detail_barang_tab').hide();
                    console.log(error);
                }
                });
            } else {
                $('#loading_detail_barang_tab').hide();
                alert("Tidak ada detail pesanan.");
            }
        }

        function refreshTableDetail() {
            if (tempBrg != null && tempBrg != undefined && tempBrg != "") {
                var link = '@Html.Raw(Url.Action("LihatDetailPesanan", "Manage", new { brgId = "replaceID", page = "replaceLastPage", search = "replaceSearch" }))';
                link = link.replace("replaceID", encodeURIComponent(tempBrg));
                link = link.replace("replaceSearch", encodeURIComponent($('#search_detail').val()));
                link = link.replace("replaceLastPage", encodeURIComponent($('#txt_last_page_detail').val()));

                $.ajax({
                    type: "GET",
                    url: link,
                    beforeSend: function () {
                        $('#list-detail').hide();
                        $('#loading_detail_barang_tab').show();
                    },
                    success: function (response) {
                        $('#list-detail').html(response);
                        $('#loading_detail_barang_tab').hide();
                        $('#list-detail').show();
                    },
                    error: function (xhr, status, error) {
                        $('#loading_detail_barang_tab').hide();
                        console.log(error);
                    }
                });
            } else {
                $('#loading_detail_barang_tab').hide();
                alert("Tidak ada detail pesanan.");
            }
        }
        //end add by nurul 29/4/2020

        //add by nurul 13/5/2020
        function LihatDetailStok($req_id_stok) {
            if ($req_id_stok != null && $req_id_stok != undefined && $req_id_stok != "") {
                var tempReqId = $req_id_stok.split('|');
            tempBrgStok = tempReqId[0];
            tempNamaStok = tempReqId[1];
            tempStok = tempReqId[2];
                $('#search_detail_stok').val('');
                $('#txt_last_page_detail_stok').val('')
        var $link = '@Html.Raw(Url.Action("LihatDetailStok", "Manage", new { brgId = "replaceID", page = "replaceLastPage", search = "replaceSearch" }))';
            $link = $link.replace("replaceID", encodeURIComponent(tempBrgStok));
            $link = $link.replace("replaceSearch", encodeURIComponent($('#search_detail_stok').val()));
            $link = $link.replace("replaceLastPage", encodeURIComponent($('#txt_last_page_detail_stok').val()));

        $.ajax({
            type: "GET",
            url: $link,
            beforeSend: function () {
                $('#list-detail-stok').hide();
                $('#loading_detail_stok_barang_tab').show();
                $('#kd-brg-stok').val(tempBrgStok);
                $('#nm-brg-stok').text(tempNamaStok);
                $('#ttl-stok').text(": " + tempStok);
            },
            success: function (value) {
                $('#list-detail-stok').html(value);
                tempGd = qtyGd;
                $('#ttl-gd').text(": " + tempGd);
                $('#loading_detail_stok_barang_tab').hide();
                $('#list-detail-stok').show();
            },
            error: function (xhr, status, error) {
                $('#loading_detail_stok_barang_tab').hide();
                alert(error);
                console.log(error);
            }
            });
        } else {
                $('#loading_detail_stok_barang_tab').hide();
            alert("Tidak ada detail stok.");
        }
        }

        function searchTableDetailStok() {
            if (tempBrgStok != null && tempBrgStok != undefined && tempBrgStok != "") {
            var link = '@Html.Raw(Url.Action("LihatDetailStok", "Manage", new { brgId = "replaceID", search = "replaceSearch" }))';
            link = link.replace("replaceID", encodeURIComponent(tempBrgStok));
                link = link.replace("replaceSearch", encodeURIComponent($('#search_detail_stok_stok').val()));

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#list-detail-stok').hide();
                    $('#loading_detail_stok_barang_tab').show();
                },
                success: function (response) {
                    $('#list-detail-stok').html(response);
                    $('#loading_detail_stok_barang_tab').hide();
                    $('#list-detail-stok').show();
                },
                error: function (xhr, status, error) {
                    $('#loading_detail_stok_barang_tab').hide();
                    console.log(error);
                }
                });
            } else {
                $('#loading_detail_stok_barang_tab').hide();
                alert("Tidak ada detail stok.");
            }
        }

        function refreshTableDetailStok() {
            if (tempBrgStok != null && tempBrgStok != undefined && tempBrgStok != "") {
                var link = '@Html.Raw(Url.Action("LihatDetailStok", "Manage", new { brgId = "replaceID", page = "replaceLastPage", search = "replaceSearch" }))';
                link = link.replace("replaceID", encodeURIComponent(tempBrgStok));
                link = link.replace("replaceSearch", encodeURIComponent($('#search_detail_stok').val()));
                link = link.replace("replaceLastPage", encodeURIComponent($('#txt_last_page_detail_stok').val()));

                $.ajax({
                    type: "GET",
                    url: link,
                    beforeSend: function () {
                        $('#list-detail-stok').hide();
                        $('#loading_detail_stok_barang_tab').show();
                    },
                    success: function (response) {
                        $('#list-detail-stok').html(response);
                        $('#loading_detail_stok_barang_tab').hide();
                        $('#list-detail-stok').show();
                    },
                    error: function (xhr, status, error) {
                        $('#loading_detail_stok_barang_tab').hide();
                        console.log(error);
                    }
                });
            } else {
                $('#loading_detail_stok_barang_tab').hide();
                alert("Tidak ada detail stok.");
            }
        }
        //end add by nurul 13/5/2020
    </script>
}