@using System.Globalization
@using PagedList.Mvc
@model PagedList.IPagedList<mdlMultiSKU>
@{
    string currentFilter = ViewBag.CurrentFilter;
    string currentSort = ViewBag.CurrentSort;

}
<style>
    #loading_MultiSKU_tab {
        display: none;
        width: 100%;
        height: 100%;
        position: fixed;
        z-index: 100;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
        background-color: rgba(0,0,0,0);
    }

    #loading_MultiSKU_tab_image {
        width: 20px;
        height: 20px;
        margin-top: -90px;
        margin-left: -90px;
        position: absolute;
        top: 50%;
        left: 50%;
        border-width: 30px;
        border-radius: 50%;
    }
</style>
<div id="loading_MultiSKU_tab">
    <div id="loading_MultiSKU_tab_image">
        <img src="~/Content/Images/spinner.gif" />
    </div>
</div>
<input id="txt_last_page" type="hidden" class="form-control" value="@ViewData["LastPage"]">
<table class="table table-striped table-bordered table-hover">
    @*<table id="datatable" class="table table-striped table-bordered dataTable" role="grid" aria-describedby="datatable_info">*@
        <thead>
            <tr role="row">
                <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" style="width: 85px;">Kode Barang Acuan</th>
                <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" style="width: 85px;">Nama Barang</th>
                <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" style="width: 85px;">Terakhir Edit</th>
                <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" style="width: 40px; max-width: 40px;">Edit</th>
                <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" style="width: 40px; max-width: 40px;">Hapus</th>
            </tr>
        </thead>
        <tbody>
            @*@foreach (var MultiSKU in Model.ListMultiSKU)*@
            @foreach (var MultiSKU in Model)
            {
                <tr>
                    <td>@MultiSKU.brg_acuan</td>
                    <td>@MultiSKU.nama</td>
                    <td>@(MultiSKU.tgl_edit?.ToString("dd/MM/yyyy"))</td>
                    <td class="edit-hapus-col">
                        @*@if (Convert.ToString(MultiSKU.POSTING) == "Y")
                        {
                            <button class="btn btn-primary" onclick="lihatMultiSKU(@MultiSKU.RECNUM)">
                                <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span>
                            </button>
                        }
                        else
                        {*@
                            <button class="btn btn-primary" onclick="editMultiSKU('@MultiSKU.brg_acuan')">
                                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                            </button>
                        @*}*@
                    </td>
                    <td class="edit-hapus-col">
                        @*@if (Convert.ToString(MultiSKU.POSTING) == "Y")
                        {
                            <button class="btn btn-danger" onclick="undeleteableMultiSKU()">
                                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                            </button>
                        }
                        else
                        {*@
                            @*<button class="btn btn-danger" data-toggle="modal" data-target="#konfHapusMultiSKU" onclick="pass('@MultiSKU.brg_acuan')">*@
                        <button class="btn btn-danger" onclick="deleteSKUKlik('@MultiSKU.brg_acuan')">
                                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                        </button>
                        @*}*@
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @* add by nurul 15/5/2019, paging *@
    @if (Model.PageCount > 1)
    {
        <div id="tabelMultiSKUpartialpager" class="pager">
            @Html.PagedListPager(Model, page => Url.Action("RefreshTableMultiSKU", new
       {
           page,
           search = ViewData["searchParam"]
       }),
            new PagedListRenderOptions
            {
                LinkToFirstPageFormat = "<<",
                LinkToPreviousPageFormat = "prev",
                LinkToNextPageFormat = "next",
                LinkToLastPageFormat = ">>",
                DisplayEllipsesWhenNotShowingAllPageNumbers = false
            })
            Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

        </div>
    }
    <script type="text/javascript">
    $(function () {
        $('#tabelMultiSKUpartialpager').on('click', 'a', function () {
            if (this.href != "") {
                $.ajax({
                    url: this.href,
                    type: 'GET',
                    cache: false,
                    beforeSend: function () {
                        $('#loading_MultiSKU_tab').show();
                    },
                    success: function (response) {
                        $('#table-MultiSKU-partial').html(response);
                        $('#loading_MultiSKU_tab').hide();
                    }
                });
            }
            return false;
        });
    });
    </script>
    @* end add by nurul 15/5/2019, paging *@
