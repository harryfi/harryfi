@model MasterOnline.ViewModels.BayarHutangViewModel
@{
    ViewBag.Title = "Pembayaran Hutang";
    Layout = "~/Views/Shared/_LayoutManage.cshtml";

    var context = new MoDbContext();
}

@section styles
{
    <link href="~/Content/build/css/bootstrap-datepicker.min.css" rel="stylesheet" />
    <link href="~/Content/build/css/selectivity-jquery.min.css" rel="stylesheet" />
    <link href="~/Content/selectize.css" rel="stylesheet" />

    <style>
        .input-group {
            margin-bottom: 0;
        }

        .btn-success[disabled]:hover {
            background: #6ad6bf;
            border-color: #6ad6bf;
        }

        table#table_tambah_hutang thead tr {
            background-color: #c7f1f5;
        }

        .qty-input {
            width: 50px;
        }

        .disc-input {
            width: 50px;
        }

        .ndisc-input {
            width: 100px;
        }

        #INVOICE {
            width: 120px !important;
        }
    </style>
}

<div class="row">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="" role="tabpanel" data-example-id="togglable-tabs">
            <ul id="myTab" class="nav nav-tabs bar_tabs" role="tablist">
                <li role="presentation" class="active">
                    <a href="#tab_content1" id="hutang-tab" role="tab" data-toggle="tab" aria-expanded="true">Pembayaran Hutang</a>
                </li>
                @*<li role="presentation" class="">
                        <a href="#tab_content2" role="tab" id="nunggu-tab" data-toggle="tab" aria-expanded="false">Menunggu Pembayaran</a>
                    </li>
                    <li role="presentation" class="">
                        <a href="#tab_content4" role="tab" id="sedangproses-tab" data-toggle="tab" aria-expanded="false">Dalam Proses</a>
                    </li>
                    <li role="presentation" class="">
                        <a href="#tab_content6" role="tab" id="sudahkirim-tab" data-toggle="tab" aria-expanded="false">Sudah Dikirim</a>
                    </li>
                    <li role="presentation" class="">
                        <a href="#tab_content7" role="tab" id="selesai-tab" data-toggle="tab" aria-expanded="false">Selesai</a>
                    </li>*@
            </ul>
            <div id="myTabContent" class="tab-content">
                <div role="tabpanel" class="tab-pane fade active in" id="tab_content1" aria-labelledby="hutang-tab">
                    <div class="row hutang_table_section">
                        <div class="col-lg-4 col-sm-6">
                            <div class="input-group">
                                <input id="search_hutang" type="text" class="form-control" placeholder="Pencarian">
                                <span class="input-group-btn">
                                    <button type="button" class="btn btn-primary">
                                        <i class="fa fa-search"></i>
                                    </button>
                                </span>
                            </div>
                        </div>
                        <div class="col-lg-8 col-sm-6">
                            <div class="pull-right">
                                <button type="button" class=" btn btn-default" data-style="expand-right">
                                    <span>Refresh</span>
                                </button>
                                <button class="btn btn-primary btn_tambah_data" id="tambah_hutang">Tambah Baru</button>
                            </div>
                        </div>
                    </div>
                    <div class="row hutang_table_section" style="margin-top: 5px;">
                        <div class="col-sm-12" id="table-hutang-partial">
                            @Html.Partial("TableBayarHutangPartial")
                        </div>
                    </div>
                    <div class="row hutang_editor_section" style="display: none;">
                        <div class="col-lg-12">
                            <div class="page-editor">
                                <h2 class="editor-title">Pembayaran Hutang</h2>
                                <span class="title-accent"></span>
                                <button id="close-editor-1" type="button" class="pull-right page-close">
                                    <span class="close-btn thick"></span>
                                </button>
                                @if (ViewData.ModelState.Any(x => x.Value.Errors.Any()))
                                {
                                    foreach (var modelError in ViewData.ModelState.SelectMany(x => x.Value.Errors))
                                    {
                                        <div class="alert alert-danger">
                                            <span class="message-error">@modelError.ErrorMessage</span>
                                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                                <span aria-hidden="true">&times;</span>
                                            </button>
                                        </div>
                                    }
                                }
                                @using (Html.BeginForm("SaveHutang", "Manage", FormMethod.Post, new { enctype = "multipart/form-data", id = "form-hutang" }))
                                {
                                    <div id="form-partial">
                                        @Html.Partial("DetailBayarHutangPartial")
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                </div>
                @*<div role="tabpanel" class="tab-pane fade" id="tab_content2" aria-labelledby="nunggu-tab">
                        <div class="row">
                            <div class="col-lg-4 col-sm-6">
                                <div class="input-group">
                                    <input id="search_nunggu" type="text" class="form-control" placeholder="Pencarian">
                                    <span class="input-group-btn">
                                        <button type="button" class="btn btn-primary">
                                            <i class="fa fa-search"></i>
                                        </button>
                                    </span>
                                </div>
                            </div>
                            <div class="col-lg-8 col-sm-6">
                                <div class="pull-right">
                                    <button type="button" class=" btn btn-default" data-style="expand-right">
                                        <span>Refresh</span>
                                    </button>
                                </div>
                            </div>
                        </div>
                        <div class="row" style="margin-top: 5px;">
                            <div class="col-sm-12">
                                <table id="datatable_nunggu" class="table table-striped table-bordered dataTable" role="grid" aria-describedby="datatable_info">
                                    <thead>
                                        <tr role="row">
                                            <th class="sorting_asc" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-sort="ascending" aria-label="Name: activate to sort column descending" style="width: 130px;">No. Order</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Position: activate to sort column ascending" style="width: 100px;">Tanggal Hutang</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Office: activate to sort column ascending" style="width: 117px;">Marketplace</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Age: activate to sort column ascending" style="width: 90px;">Pembeli</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Start date: activate to sort column ascending" style="width: 115px;">Total</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Salary: activate to sort column ascending" style="width: 50px; max-width: 50px;">Edit</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Status: activate to sort column ascending" style="width: 50px; max-width: 50px;">Hapus</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var hutang in Model.ListHutang)
                                        {
                                            var pemesanRecNum = Convert.ToInt32(hutang.NAMA);
                                            var buyer = Model.ListPembeli.Single(m => m.RecNum == pemesanRecNum);
                                            var marketCust = Convert.ToInt32(hutang.SUPP);
                                            var pelanggan = Model.ListPelanggan.Single(m => m.RecNum == marketCust);
                                            var idMarket = Convert.ToInt32(pelanggan.NAMA);
                                            <tr>
                                                <td>@hutang.INV</td>
                                                <td>@hutang.TGL</td>
                                                <td>@Model.ListMarketplace.Single(m => m.IdMarket == idMarket).NamaMarket</td>
                                                <td>
                                                    @(buyer.NAMA + " (" + buyer.PERSO + ")")
                                                </td>
                                                <td>@String.Format(CultureInfo.CreateSpecificCulture("id-id"), "{0:N}", hutang.NETTO)</td>
                                                <td class="edit-hapus-col">
                                                    <button class="btn btn-primary" onclick="editHutang(@hutang.RecNum)">
                                                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                                                    </button>
                                                </td>
                                                <td class="edit-hapus-col">
                                                    <button class="btn btn-danger" data-toggle="modal" data-target="#konfHapusHutang" onclick="pass(@hutang.RecNum)">
                                                        <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                                                    </button>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                    <div role="tabpanel" class="tab-pane fade" id="tab_content4" aria-labelledby="sedangproses-tab">
                        <div class="row">
                            <div class="col-lg-4 col-sm-6">
                                <div class="input-group">
                                    <input id="search_sedangproses" type="text" class="form-control" placeholder="Pencarian">
                                    <span class="input-group-btn">
                                        <button type="button" class="btn btn-primary">
                                            <i class="fa fa-search"></i>
                                        </button>
                                    </span>
                                </div>
                            </div>
                            <div class="col-lg-8 col-sm-6">
                                <div class="pull-right">
                                    <button type="button" class=" btn btn-default" data-style="expand-right">
                                        <span>Refresh</span>
                                    </button>
                                </div>
                            </div>
                        </div>
                        <div class="row" style="margin-top: 5px;">
                            <div class="col-sm-12">
                                <table id="datatable_sedangproses" class="table table-striped table-bordered dataTable" role="grid" aria-describedby="datatable_info">
                                    <thead>
                                        <tr role="row">
                                            <th class="sorting_asc" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-sort="ascending" aria-label="Name: activate to sort column descending" style="width: 130px;">No. Pengambilan</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Position: activate to sort column ascending" style="width: 140px;">No. Hutang</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Office: activate to sort column ascending" style="width: 117px;">Tgl. Hutang</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Age: activate to sort column ascending" style="width: 60px;">Pelanggan</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Start date: activate to sort column ascending" style="width: 90px;">Lokasi</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Salary: activate to sort column ascending" style="width: 90px;">Sumber</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Status: activate to sort column ascending" style="width: 120px;">Status Pengambilan</th>
                                        </tr>
                                    </thead>
                                    <tbody></tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                    <div role="tabpanel" class="tab-pane fade" id="tab_content6" aria-labelledby="sudahkirim-tab">
                        <div class="row">
                            <div class="col-lg-4 col-sm-6">
                                <div class="input-group">
                                    <input id="search_sudahkirim" type="text" class="form-control" placeholder="Pencarian">
                                    <span class="input-group-btn">
                                        <button type="button" class="btn btn-primary">
                                            <i class="fa fa-search"></i>
                                        </button>
                                    </span>
                                </div>
                            </div>
                            <div class="col-lg-8 col-sm-6">
                                <div class="pull-right">
                                    <button type="button" class=" btn btn-default" data-style="expand-right">
                                        <span>Refresh</span>
                                    </button>
                                </div>
                            </div>
                        </div>
                        <div class="row" style="margin-top: 5px;">
                            <div class="col-sm-12">
                                <table id="datatable_sudahkirim" class="table table-striped table-bordered dataTable" role="grid" aria-describedby="datatable_info">
                                    <thead>
                                        <tr role="row">
                                            <th class="sorting_asc" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-sort="ascending" aria-label="Name: activate to sort column descending" style="width: 130px;">No. Hutang</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Position: activate to sort column ascending" style="width: 259px;">Tgl. Hutang</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Office: activate to sort column ascending" style="width: 117px;">Sumber</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Age: activate to sort column ascending" style="width: 60px;">Pelanggan</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Start date: activate to sort column ascending" style="width: 115px;">Alamat Kirim</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Salary: activate to sort column ascending" style="width: 90px;">Kurir</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Status: activate to sort column ascending" style="width: 90px;">Resi</th>
                                        </tr>
                                    </thead>
                                    <tbody></tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                    <div role="tabpanel" class="tab-pane fade" id="tab_content7" aria-labelledby="selesai-tab">
                        <div class="row">
                            <div class="col-lg-4 col-sm-6">
                                <div class="input-group">
                                    <input id="search_selesai" type="text" class="form-control" placeholder="Pencarian">
                                    <span class="input-group-btn">
                                        <button type="button" class="btn btn-primary">
                                            <i class="fa fa-search"></i>
                                        </button>
                                    </span>
                                </div>
                            </div>
                            <div class="col-lg-8 col-sm-6">
                                <div class="pull-right">
                                    <button type="button" class=" btn btn-default" data-style="expand-right">
                                        <span>Refresh</span>
                                    </button>
                                </div>
                            </div>
                        </div>
                        <div class="row" style="margin-top: 5px;">
                            <div class="col-sm-12">
                                <table id="datatable_selesai" class="table table-striped table-bordered dataTable" role="grid" aria-describedby="datatable_info">
                                    <thead>
                                        <tr role="row">
                                            <th class="sorting_asc" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-sort="ascending" aria-label="Name: activate to sort column descending" style="width: 130px;">Nomor</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Position: activate to sort column ascending" style="width: 100px;">Tanggal Hutang</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Office: activate to sort column ascending" style="width: 117px;">Pelanggan</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Age: activate to sort column ascending" style="width: 60px;">Lokasi</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Start date: activate to sort column ascending" style="width: 115px;">Nilai</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Salary: activate to sort column ascending" style="width: 90px;">Sumber</th>
                                            <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Status: activate to sort column ascending" style="width: 90px;">Status Market</th>
                                        </tr>
                                    </thead>
                                    <tbody></tbody>
                                </table>
                            </div>
                        </div>
                    </div>*@
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="konfHapusHutang" tabindex="-1" role="dialog" aria-labelledby="konfirmasiHapusLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="konfirmasiHapusLabel">Konfirmasi</h4>
            </div>
            <div class="modal-body">
                <div class="row text-center">
                    <h4>Apakah Anda yakin ingin menghapus hutang ini?</h4>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Tidak</button>
                <button type="button" class="btn btn-success" onclick="deleteHutang();">Ya</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="konfHapusDetail" tabindex="-1" role="dialog" aria-labelledby="konfirmasiHapusLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="konfirmasiHapusLabel">Konfirmasi</h4>
            </div>
            <div class="modal-body">
                <div class="row text-center">
                    <h4>Apakah Anda yakin ingin menghapus invoice ini?</h4>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Tidak</button>
                <button type="button" class="btn btn-success" onclick="deleteDetail();">Ya</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="modalTutupEditor" tabindex="-1" role="dialog" aria-labelledby="labelModalTutupEditor">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="labelModalTutupEditor">Konfirmasi</h4>
            </div>
            <div class="modal-body">
                <div class="row text-center">
                    <h4>Ada perubahan data yang belum Anda simpan. <br />Yakin ingin menutup form ini?</h4>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Tidak</button>
                <button type="button" class="btn btn-success" onclick="closeEditor()">Ya</button>
            </div>
        </div>
    </div>
</div>

@section scripts
{
    <script src="//cdn.datatables.net/plug-ins/1.10.11/sorting/date-eu.js" type="text/javascript"></script>
    <script src="~/Content/build/js/bootstrap-datepicker.min.js" type="text/javascript"></script>
    <script src="~/Content/build/js/selectivity-jquery.min.js" type="text/javascript"></script>
    <script src="~/Content/vendors/moment/min/moment.min.js" type="text/javascript"></script>
    <script src="~/Content/selectize.js" type="text/javascript"></script>
    <script src='@Url.Content("~/Scripts/jquery.validate.js")' type='text/javascript'></script>
    <script src='@Url.Content("~/Scripts/jquery.validate.unobtrusive.js")' type='text/javascript'></script>

    <script type="text/javascript">
        //add by nurul 12/2/2019
        var $page = window.text;
            //end add by nurul 12/2/2019
        $(document).ready(function () {
            //add by nurul 12/2/2019
            $page = 0;
            //end add by nurul 12/2/2019
            var tableSiapProses = $('#datatable_siapproses').DataTable({
                columnDefs: [{
                    orderable: false,
                    className: 'select-checkbox',
                    targets: 0
                }],
                select: {
                    style: 'os',
                    selector: 'td:first-child'
                }
            });

            $('#search_siapproses').keyup(function () {
                tableSiapProses.search($(this).val()).draw();
            });

            Initialize();
            //add by nurul 12/2/2019
            $('#datatable').on('page.dt', function () {
                $page = $('#datatable').DataTable().page.info().page;
            });
            $('#datatable').DataTable().page($page).draw(false);
                    //end add by nurul 12/2/2019
        });

        var table = $('#datatable').DataTable({
            "order": [[0,1, "desc"]], //or asc
            "columnDefs": [{ "targets": 1, "type": "date-eu" }],
        });

        function closeEditor() {
            $('#modalTutupEditor').modal('hide');
            $('input, select, .btn-danger').each(function () {
                $(this).removeAttr('disabled');
            });
            $('.hutang_table_section').show();
            $('.hutang_editor_section').hide();
            refreshForm1();
            refreshTableHutang1();
            //add by nurul 12/2/2019
            $('#datatable').on('page.dt', function () {
                $page = $('#datatable').DataTable().page.info().page;
            });
            $('#datatable').DataTable().page($page).draw(false);
                    //end add by nurul 12/2/2019
        }

        function Initialize() {
            var table = $('#datatable').DataTable();
            //var tableNunggu = $('#datatable_nunggu').DataTable();
            //var tableSedangProses = $('#datatable_sedangproses').DataTable();
            //var tableSiapKirim = $('#datatable_siapkirim').DataTable();
            //var tableSudahKirim = $('#datatable_sudahkirim').DataTable();
            //var tableSelesai = $('#datatable_selesai').DataTable();

            $('#search_hutang').keyup(function () {
                table.search($(this).val()).draw();
            });

            //$('#search_selesai').keyup(function () {
            //    tableSelesai.search($(this).val()).draw();
            //});

            //$('#search_nunggu').keyup(function () {
            //    tableNunggu.search($(this).val()).draw();
            //});

            //$('#search_sedangproses').keyup(function () {
            //    tableSedangProses.search($(this).val()).draw();
            //});

            //$('#search_siapkirim').keyup(function () {
            //    tableSiapKirim.search($(this).val()).draw();
            //});

            //$('#search_sudahkirim').keyup(function () {
            //    tableSudahKirim.search($(this).val()).draw();
            //});

            //$('input[name="select_all"]').on('click',
            //    function () {
            //        var attr = $(this).attr('checked');

            //        if (typeof attr !== typeof undefined && attr !== false) {
            //            $('input[name="data-1"]').removeAttr('checked');
            //            $('input[name="data-2"]').removeAttr('checked');
            //        } else {
            //            $('input[name="data-1"]').attr('checked', 'checked');
            //            $('input[name="data-2"]').attr('checked', 'checked');
            //        }
            //    });

            $('#tambah_hutang').on('click', function () {
                $('.hutang_table_section').hide();
                $('.hutang_editor_section').show();
            });

            $('#close-editor-1').on('click', function () {
                if ($('#btn-simpan-perubahan-row').is(':visible')) {
                    $('#modalTutupEditor').modal('show');
                } else {
                    $('input, select, .btn-danger').each(function () {
                        $(this).removeAttr('disabled');
                    });
                    $('.hutang_table_section').show();
                    $('.hutang_editor_section').hide();
                    refreshForm1();
                    refreshTableHutang1();
                    //add by nurul 12/2/2019
                    $('#datatable').on('page.dt', function () {
                        $page = $('#datatable').DataTable().page.info().page;
                    });
                    $('#datatable').DataTable().page($page).draw(false);
                    //end add by nurul 12/2/2019
                }
            });

            if ($('.message-error').text() !== '') {
                $('#tambah_hutang').click();
            }

            $('#table_tambah_hutang > tbody > tr > td.editable-kode').prop('contentEditable', true);

            getTanggal();
            getSupplier();
            getInvoice(null);

            if ($('#Hutang_TGL').val() != '') {
                $('#TGL').val($('#Hutang_TGL').val());
            } else {
                $('#Hutang_TGL').val($('#TGL').val());
            }

            refreshInputan();
            //add by nurul 12/2/2019
            $('#datatable').on('page.dt', function () {
                $page = $('#datatable').DataTable().page.info().page;
            });
            $('#datatable').DataTable().page($page).draw(false);
                    //end add by nurul 12/2/2019
        }

        function getTanggal() {
            $('#TGL').datepicker({
                format: 'dd/mm/yyyy',
                language: 'id'
            }).datepicker('setDate', '0').change(function () {
                $('#Hutang_TGL').val($(this).val());
                //add by nurul 31/10/2018
                $(this).datepicker('hide');
                //end add
                });


            if ($('#Hutang_TGL').val() != '') {
                $('#TGL').val($('#Hutang_TGL').val().substr(0, 10));
            } else {
                $('#Hutang_TGL').val($('#TGL').val());
            }
        }

        var $flagPerubahan = 0;

        function refreshInputan() {
            if ($('#Hutang_tgl').val() != '') {
                $('#TGL').val($('#Hutang_TGL').val());
            }

            $('#TOTALBAYAR').attr('readonly', 'readonly').css('background-color', '#eee')
                .css('cursor', 'not-allowed');
            //ADD BY NURUL 10/10/2018
            $('#TOTALBYR').attr('readonly', 'readonly').css('background-color', '#eee')
                .css('cursor', 'not-allowed');
            $('#TOTALPOT').attr('readonly', 'readonly').css('background-color', '#eee')
                .css('cursor', 'not-allowed');
            //END ADD 

            // ==== Update Data ====
            $('#Hutang_TGL').change(function () {
                if ($flagPerubahan == 1) {
                    $('#btn-simpan-perubahan-row').show();
                }

                $flagPerubahan++;
            });

            $('#btn-simpan-perubahan').click(function () {
                var $this = $(this);
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("UpdateBayarHutang", "Manage")',
                    data: JSON.stringify({
                        OrderId: "@(Model?.Hutang?.BUKTI)",
                        Tgl: $('#Hutang_tgl').val()
                    }),
                    beforeSend: function () {
                        $('#loading_spinner').show();
                    },
                    contentType: 'application/json; charset=UTF-8',
                    cache: false,
                    success: function () {
                        alert('Data hutang berhasil di-update');
                        $this.hide();
                        $('#loading_spinner').hide();
                    },
                    error: function (xhr) {
                        console.log(xhr);
                        $('#loading_spinner').hide();
                    }
                });
            });
            
            //var $totalPot = 0;
            //var $totalBayar = 0;
            //var $totalSemua = 0;

            //$('tr[data-hutang-id="0"] #HutangDetail_POT').change(function () {
            //    //$totalSemua = parseInt($('#HutangDetail_POT').val()) + parseInt($('#HutangDetail_BAYAR').val());
            //    $totalPot = convertToAngka($('#TOTALPOT').val()) + parseInt($('#HutangDetail_POT').val());
            //    $('tr[data-hutang-id="0"] .span-potongan-hutang').text(convertToRupiah($(this).val()));
            //    $('#TOTALPOT').val(convertToRupiah($totalPot));
            //    $totalSemua = $totalPot + convertToAngka($('#TOTALBYR').val());
            //    $('#TOTALBAYAR').val(convertToRupiah($totalSemua));
            ////}).focus(function () {
            ////    $(this).val('');
            //}).blur(function() {
            //    if ($(this).val() == '')
            //        $(this).val(0);
            //});

            //$('tr[data-hutang-id="0"] #HutangDetail_BAYAR').change(function () {
            //    $totalBayar = convertToAngka($('#TOTALBYR').val()) + parseInt($('#HutangDetail_BAYAR').val());
            //    $('tr[data-hutang-id="0"] .span-bayar-hutang').text(convertToRupiah($(this).val()));
            //    $('#TOTALBYR').val(convertToRupiah($totalBayar));
            //    $totalSemua = $totalBayar + convertToAngka($('#TOTALPOT').val());
            //    $('#TOTALBAYAR').val(convertToRupiah($totalSemua));
            ////}).focus(function () {
            ////    $(this).val('');
            //}).blur(function () {
            //    if ($(this).val() == '')
            //        $(this).val(0);
            //});

            //add by nurul 12/2/2019
            $('#datatable').on('page.dt', function () {
                $page = $('#datatable').DataTable().page.info().page;
            });
            $('#datatable').DataTable().page($page).draw(false);
                    //end add by nurul 12/2/2019
        }

        function getSupplier() {
            $.ajax({
                type: "GET",
                url: '@Url.Action("GetSupplier", "Manage")',
                contentType: 'application/json',
                cache: false,
                success: function (data) {
                    var supplierList = [];

                    $.each(data,
                        function (i, item) {
                            supplierList[i] = {
                                id: item.SUPP,
                                text: item.NAMA
                            };
                        });

                    var custSelect = $('#SUPPLIER').selectize({
                        valueField: 'id',
                        searchField: 'text',
                        options: supplierList,
                        onChange: function(value) {
                            if (value == null) {
                                $("#Hutang_SUPP").val(null);
                                getInvoice(null);
                            } else {
                                $("#Hutang_SUPP").val(value);
                                getInvoice(value);
                            }
                        }
                    });

                    custSelect[0].selectize.setValue($('#Hutang_SUPP').val());
                },
                error: function (xhr) {
                    console.log(xhr);
                }
            });
        }

        function getInvoice($supplierId) {
            if ($supplierId == null) {
                $('#INVOICE').selectivity({
                    allowClear: true,
                    items: [],
                    placeholder: 'Harap pilih'
                });
            } else {
                var $link = '@Url.Action("GetInvoiceBelumLunas", "Manage", new { noSupp = "replaceId" })';
                $link = $link.replace("replaceId", $supplierId);

                $.ajax({
                    type: "GET",
                    url: $link,
                    contentType: 'application/json',
                    cache: false,
                    success: function (data) {
                        var listKodeInvoice = [];

                        $.each(data,
                            function (i, item) {
                                listKodeInvoice[i] = {
                                    id: item.RecNum,
                                    text: item.INV
                                };
                            });

                        $('#INVOICE').selectivity({
                            allowClear: true,
                            items: listKodeInvoice,
                            placeholder: 'Harap pilih'
                        }).change(function () {
                            var val = $(this).selectivity('value');
                            var $trId = $(this).closest('tr').attr('data-hutang-id');
                            var $trElement = 'tr[data-hutang-id="' + $trId + '"]';

                            if (val != null) {
                                $.each(data,
                                    function (i, item) {
                                        if (item.RecNum == val) {
                                            $($trElement + ' .span-sisa-hutang').text(convertToRupiah(item.Sisa));
                                            $('#HutangDetail_SISA').val(item.Sisa);
                                            $('#HutangDetail_NINV').val(item.INV);
                                            return;
                                        }
                                    });

                                $($trElement + ' .btn-primary').removeAttr('disabled');
                                $($trElement + ' .span-potongan-hutang').hide();
                                $($trElement + ' .span-bayar-hutang').hide();
                                $($trElement + ' #HutangDetail_POT').show();
                                $($trElement + ' #HutangDetail_BAYAR').show();
                            } else {
                                $($trElement + ' .btn-primary').attr('disabled', 'disabled');
                                $($trElement + ' .span-potongan-hutang').val('-').show();
                                $($trElement + ' .span-bayar-hutang').val('-').show();
                                $($trElement + ' #HutangDetail_POT').show();
                                $($trElement + ' #HutangDetail_BAYAR').show();

                                @if (Model?.ListHutangDetail?.Count == 0)
                                {
                                    <text>
                                        $('#Hutang_TBAYAR').val(@(Model?.Hutang?.TBAYAR));
                                    </text>
                                }
                            }
                        });
                    },
                    error: function (xhr) {
                        console.log(xhr);
                    }
                });
            }
        }

        function convertToRupiah(angka) {
            //change by calvin 26 september 2018
            //var rupiah = '';
            //var angkarev = angka.toString().split('').reverse().join('');
            //for (var i = 0; i < angkarev.length; i++) if (i % 3 === 0) rupiah += angkarev.substr(i, 3) + '.';
            //return rupiah.split('', rupiah.length - 1).reverse().join('') + ',00';
            return $.number(angka, 2, ',', '.');
            //end change by calvin 26 september 2018
        }

        function convertToAngka(rupiah) {

            if (rupiah == '') {
                rupiah = '0';
            }
            //change by calvin 26 september 2018
            //return parseInt(rupiah.replace(/,.*|[^0-9]/g, ''), 10);
            if (rupiah.includes(",")) {
                return parseFloat(rupiah.replaceAll('.', '').replaceAll(',', '.'));
            }
            else {
                //jika bisa desimal
                return parseFloat(rupiah.replaceAll(',', '.'));
            }
            //end change by calvin 26 september 2018
        }

        var $idHutang = "";

        function undeleteable() {
            alert('Transaksi Sudah Posting! \nTidak dapat dihapus!');
        }
        function lihatHutang(idHutang) {
            var link = '@Url.Action("EditBayarHutang", "Manage", new { orderId = "replaceId" })';
            link = link.replace("replaceId", idHutang);

            $.ajax({
                type: "GET",
                url: link,
                success: function (response) {
                    $('#form-partial').html(response);
                    refreshInputan();
                    getTanggal();
                    getSupplier();
                    getInvoice($("#Hutang_SUPP").val());
                    $('#tambah_hutang').click();
                    $('input, select, .btn-danger').each(function () {
                        $(this).attr('disabled', 'disabled');
                    });
                    $('tr[data-hutang-id="0"]').remove();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }
        function editHutang1(idHutang) {
            var link = '@Url.Action("EditBayarHutang", "Manage", new { orderId = "replaceId" })';
            link = link.replace("replaceId", idHutang);

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#loading_spinner').show();
                },
                success: function (response) {
                    $('#form-partial').html(response);
                    refreshInputan();
                    getTanggal();
                    getSupplier();
                    getInvoice($("#Hutang_SUPP").val());
                    $('#tambah_hutang').click();
                    $('#loading_spinner').hide();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                    $('#loading_spinner').hide();
                }
            });
        }

        function pass(idHutang) {
            this.$idHutang = idHutang;
        }

        function deleteHutang() {
            var link = '@Url.Action("DeleteBayarHutang", "Manage", new { orderId = "replaceId" })';
            link = link.replace("replaceId", this.$idHutang);

            $.ajax({
                type: "GET",
                url: link,
                beforeSend: function () {
                    $('#loading_spinner').show();
                },
                success: function (response) {
                    $('#table-hutang-partial').html(response);
                    var table = $('#datatable').DataTable({
                        "order": [[0, 1, "desc"]], //or asc
                        "columnDefs": [{ "targets": 1, "type": "date-eu" }],
                    });
                    Initialize();
                    $('#konfHapusHutang').modal('hide');
                    $('#loading_spinner').hide();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                    $('#loading_spinner').hide();
                }
            });
        }

        var $inv = '';

        function passInvoiceInDb($inv) {
            this.$inv = $inv;
        }

        function deleteDetail() {
            var link = '@Url.Action("DeleteDetailBayarHutang", "Manage", new { noUrut = "replaceInvId" })';
            link = link.replace("replaceInvId", this.$inv);

            $.ajax({
                type: "GET",
                url: link,
                contentType: 'application/json',
                cache: false,
                beforeSend: function () {
                    $('#loading_spinner').show();
                },
                success: function (response) {
                    $('#form-partial').html(response);
                    refreshInputan();
                    getTanggal();
                    getSupplier();
                    getInvoice($("#Hutang_SUPP").val());
                    $('#konfHapusDetail').modal('hide');
                    $('#loading_spinner').hide();
                },
                error: function (xhr) {
                    console.log(xhr);
                    $('#loading_spinner').hide();
                }
            });
        }

        function refreshTableHutang1() {
            var link = '@Url.Action("RefreshTableBayarHutang1", "Manage")';

            $.ajax({
                type: "GET",
                url: link,
                success: function (response) {
                    $('#table-hutang-partial').html(response);
                    var table = $('#datatable').DataTable({
                        "order": [[0,1, "desc"]], //or asc
                        "columnDefs": [{ "targets": 1, "type": "date-eu" }],
                    });
                    $('#search_hutang').keyup(function () {
                        table.search($(this).val()).draw();
                    });
                    //add by calvin 23 nov 2018, setelah di close, search lg
                    table.search($('#search_hutang').val()).draw();
                    //end add by calvin 23 nov 2018, setelah di close, search lg
                    //add by nurul 12/2/2019
                    $('#datatable').on('page.dt', function () {
                        $page = $('#datatable').DataTable().page.info().page;
                    });
                    $('#datatable').DataTable().page($page).draw(false);
                    //end add by nurul 12/2/2019
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }

        function refreshForm1() {
            var link = '@Url.Action("RefreshBayarHutangForm", "Manage")';

            $.ajax({
                type: "GET",
                url: link,
                success: function (response) {
                    $('#form-partial').html(response);
                    refreshInputan();
                    getTanggal();
                    getSupplier();
                    getInvoice(null);
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        }

        function simpandetail($id) {
            if (!validateForm()) return false;

            if ($id == 'hut-0') {
                if ($('#Hutang_TGL').val() != '') {
                    $('#Hutang_TGLINPUT').val(moment(new Date()).format("DD/MM/YYYY"));
                    $('#HutangDetail_NFAKTUR').val($('#INVOICE').selectivity('val'));
                    $('#HutangDetail_SISA').val(convertToAngka($('tr[data-hutang-id="0"] .span-sisa-hutang').text()));
                    //$('#HutangDetail_POT').val(convertToAngka($('tr[data-hutang-id="0"] .span-potongan-hutang').text()));
                    //$('#HutangDetail_BAYAR').val(convertToAngka($('tr[data-hutang-id="0"] .span-bayar-hutang').text()));
                    if ($('#HutangDetail_POT').val() == "") {
                        $('#HutangDetail_POT').val("0") ;
                        //$('#HutangDetail_POT').val(convertToAngka($('#HutangDetail_POT').val()));
                        $('#HutangDetail_POT').val(parseFloat($('#HutangDetail_POT').val()));
                    } else {
                        //$('#HutangDetail_POT').val(convertToAngka($('#HutangDetail_POT').val()));
                        $('#HutangDetail_POT').val(parseFloat($('#HutangDetail_POT').val()));
                    }
                    if ($('#HutangDetail_BAYAR').val() == "") {
                        $('#HutangDetail_BAYAR').val("0") ;
                        //$('#HutangDetail_BAYAR').val(convertToAngka($('#HutangDetail_BAYAR').val()));
                        $('#HutangDetail_BAYAR').val(parseFloat($('#HutangDetail_BAYAR').val()));
                    } else {
                        //$('#HutangDetail_BAYAR').val(convertToAngka($('#HutangDetail_BAYAR').val()));
                        $('#HutangDetail_BAYAR').val(parseFloat($('#HutangDetail_BAYAR').val()));
                    }

                    //var sum = parseInt($('#HutangDetail_POT').val()) + parseInt($('#HutangDetail_BAYAR').val());
                    var sum = parseFloat($('#HutangDetail_POT').val()) + parseFloat($('#HutangDetail_BAYAR').val());

                    //if (parseInt($('#HutangDetail_SISA').val()) < sum) {
                    if (parseFloat($('#HutangDetail_SISA').val()) < sum) {
                        alert('Nilai Potongan + Nilai Bayar tidak boleh melebihi Nilai Sisa!');
                        return;
                    }

                    //$('#Hutang_TPOT').val(parseInt($('#Hutang_TPOT').val()) + parseInt($('#HutangDetail_POT').val()));
                    //$('#Hutang_TBAYAR').val(parseInt($('#Hutang_TBAYAR').val()) + parseInt($('#HutangDetail_BAYAR').val()));
                    //$('#TotalBAYAR').val(parseInt($('#Hutang_TPOT').val()) + parseInt($('#Hutang_TBAYAR').val()));
                    $('#Hutang_TPOT').val(parseFloat($('#Hutang_TPOT').val()) + parseFloat($('#HutangDetail_POT').val()));
                    $('#Hutang_TBAYAR').val(parseFloat($('#Hutang_TBAYAR').val()) + parseFloat($('#HutangDetail_BAYAR').val()));
                    $('#TotalBAYAR').val(parseFloat($('#Hutang_TPOT').val()) + parseFloat($('#Hutang_TBAYAR').val()));



                    $.ajax({
                        type: "POST",
                        url: '@Url.Action("SaveBayarHutang", "Manage")',
                        data: $('#form-hutang').serialize(),
                        beforeSend: function () {
                            $('#loading_spinner').show();
                        },
                        success: function (response) {
                            if (response.Errors == null) {
                                $('#form-partial').html(response);
                                refreshInputan();
                                getTanggal();
                                getSupplier();
                                getInvoice($("#Hutang_SUPP").val());
                                $('#btn-simpan-perubahan-row').hide();
                                $('#loading_spinner').hide();
                            } else {
                                console.log(response.Errors);
                                $('#loading_spinner').hide();
                                alert('Ada data yang tidak valid!');
                            }
                        },
                        error: function (xhr) {
                            console.log(xhr);
                        }
                    });
                } else {
                    alert('Silakan isi semua field terlebih dahulu!');
                }
            }
        }
        //add by Tri function to call bca api
        function closeFormAfterTransfer() {
            $('input, select, .btn-danger').each(function () {
                $(this).removeAttr('disabled');
            });
            $('.hutang_table_section').show();
            $('.hutang_editor_section').hide();
            refreshForm1();
            refreshTableHutang1();
        }

        function popTransfer(sTransfer, noBuk) {
            if (sTransfer == '1') {
                //sudah transfer
                alert("Transaksi ini sudah dilakukan transfer antar bank");
            } else {
                var link = '@Url.Action("GetDataTransfer", "BCA")';
                link += "?nobuk=" + noBuk;
                $.ajax({
                    type: "GET",
                    url: link,
                    contentType: 'application/json',
                    cache: false,
                    success: function (data) {
                        if (data.status == 1) {
                            document.getElementById('txtNoRekFrom').value = data.SourceAccountNumber;
                            document.getElementById('txtNamaFrom').value = data.SourceAccountName;
                            document.getElementById('txtCorpId').value = data.CorporateID;
                            document.getElementById('txtNoRek').value = data.BeneficiaryAccountNumber;
                            document.getElementById('txtNamaTo').value = data.BeneficiaryAccountName;

                            $('#modalDataRekBCA').modal('show');
                        }
                        else {
                            alert(data.message);
                        }

                    },
                    error: function (xhr) {
                        console.log(xhr);
                    }
                });
            }
        }

        function TransferBCA(sNoBuk, sSupp, sAmount, sRekNo, sCorpID) {
            //var link = '@Url.Action("TransferBCA", "Manage", new { amount = "replaceAmount", corpId = "replaceCorpID", formRek = "replaceRek", reffID = "replaceNoBuk", supp = "replaceSupp" })';
            var link = '@Url.Action("TransferBCA", "BCA")';
            link = link + "?amount=" + sAmount + "&corpId=" + sCorpID + "&formRek=" + sRekNo + "&reffID=" + sNoBuk +"&supp=" + sSupp;
            //link = link.replace("replaceAmount", sAmount);
            //link = link.replace("replaceCorpID", sCorpID);
            //link = link.replace("replaceRek", sRekNo);
            //link = link.replace("replaceNoBuk", sNoBuk);
            //link = link.replace("replaceSupp", sSupp);

                 $.ajax({
                    type: "GET",
                    url: link,
                    contentType: 'application/json',
                     cache: false,
                     success: function (data) {
                         if (data.status == 0) {
                             alert('Error!' + data.message);
                         } else {
                             alert('Transfer sukses!');
                             $('#modalDataRekBCA').modal('hide');
                             closeFormAfterTransfer();
                         }
                         //popMsg(data);
                     },
                    error: function (xhr) {
                        console.log(xhr);
                    }
                });

        }

        //end add by Tri function to call bca api

    </script>

    <script>
        //Disable Enter Key Submit Form
        function stopRKey(evt) {
            var evt = (evt) ? evt : ((event) ? event : null);
            var node = (evt.target) ? evt.target : ((evt.srcElement) ? evt.srcElement : null);
            if ((evt.keyCode === 13) && (node.type === "text")) { return false; }
        }
        document.onkeypress = stopRKey;
    </script>
}